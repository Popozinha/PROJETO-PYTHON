from math import  floor, ceil
raiz = float(input('digite um numero: '))
f = (float(floor(raiz)))
print(f'Seu numero {raiz} e o seu numero inteiro é {f}')

import random

lista = [1, 2, 3, 4]
sorteio = random.random(lista)
#lista = sorteio
print (sorteio)

